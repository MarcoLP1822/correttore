# .gitignore - Guida

Questo file `.gitignore` è configurato per la struttura Clean Architecture del progetto.

## 📋 Categorie Ignorate

### 🐍 Python
- `__pycache__/` - Cache bytecode Python
- `*.pyc`, `*.pyo`, `*.pyd` - File compilati
- `*.egg-info/` - Metadata pacchetti
- `.pytest_cache/` - Cache pytest
- `.mypy_cache/` - Cache mypy
- `.coverage` - Report coverage

### 🌍 Environment
- `.venv/`, `venv/` - Virtual environments
- `.env`, `.env.local` - Variabili ambiente (contengono secrets!)
- `.env.*.local` - Environment locali

### 📦 Build & Distribution
- `build/`, `dist/` - Build artifacts
- `*.egg`, `*.whl` - Package files

### 📁 Project Specific

#### Output Directories
- `uploads/` - File caricati via web
- `outputs/` - Documenti corretti
- `backups/` - Backup automatici
- `test_output/` - Output test

#### Cache
- `.cache/` - Cache generale
- `storage/cache/` - Cache intelligente
- `.llm_cache.pkl` - Cache LLM

#### Large Files
- `languagetool/` - LanguageTool binaries (>100MB)
- `_migration_backup_*/` - Backup migrazioni

#### Old Structure (Post-Migration)
Commentate finché non si verifica che la nuova struttura funzioni:
```gitignore
# /core/
# /services/
# /utils/
# /config/
# /tools/
```

### 📝 Documentazione
- `docs/_build/` - Sphinx builds
- `docs/*_OLD.md` - Backup vecchi
- `site/` - MkDocs builds

### 🧪 Testing
- `.pytest_cache/`
- `.coverage`, `htmlcov/`
- `tests/fixtures/*.docx` - File test generati

### 🔐 Secrets (IMPORTANTE!)
Mai committare:
- `.env`, `.env.local`
- `*.pem`, `*.key`
- `secrets.yaml`, `secrets.json`
- `*_api_key.txt`

### 💻 IDE
- `.vscode/settings.json` - Settings personali
- `.vscode/launch.json` - Config debug personali
- `.idea/` - PyCharm

### 🖥️ OS
- `.DS_Store` - macOS
- `Thumbs.db` - Windows
- `*~` - Linux backup files

## ✅ File Tracciati

### Root
- `README.md`, `CHANGELOG.md`, `CONTRIBUTING.md`
- `setup.py`, `pyproject.toml`
- `requirements.txt`
- `config.yaml` (config template)
- `.env.example` (template, NO secrets!)
- `.gitignore`

### Source Code
- `src/correttore/**/*.py`
- `scripts/**/*.py`
- `tests/**/*.py`

### Documentation
- `docs/**/*.md`
- `docs/images/**`

### Data
- `data/glossari/*.txt`
- `data/corrections/*.txt`
- Template e config di base

### Examples
- `examples/.gitkeep` (mantiene struttura)
- File esempio possono essere ignorati se grandi

## 🔄 Dopo Migrazione

Una volta verificato che la nuova struttura Clean Architecture funzioni:

1. **Decommentare nel .gitignore:**
```gitignore
/core/
/services/
/utils/
/config/
/tools/
correttore files/
```

2. **Rimuovere cartelle vecchie:**
```bash
rm -rf core/ services/ utils/ config/ tools/
rm -rf "correttore files/"
```

3. **Commit changes:**
```bash
git add .
git commit -m "chore: remove old structure after migration"
```

## 🧹 Pulizia Repository

Se il repository ha già tracciato file che dovrebbero essere ignorati:

```bash
# Rimuovi dalla cache git (mantiene file localmente)
git rm -r --cached .

# Ri-aggiungi rispettando .gitignore
git add .

# Verifica cosa è stato rimosso
git status

# Commit
git commit -m "chore: update .gitignore and remove tracked files"
```

## ⚠️ Attenzione

### Mai Committare:
- ❌ API keys o tokens
- ❌ File `.env` con secrets
- ❌ Credentials di qualsiasi tipo
- ❌ File grandi (>50MB) senza Git LFS
- ❌ File binari compilati
- ❌ Cache o file temporanei

### Sempre Committare:
- ✅ Source code (`.py`)
- ✅ Documentazione (`.md`)
- ✅ Template config (`.env.example`)
- ✅ Requirements (`requirements.txt`)
- ✅ Tests
- ✅ Scripts di setup

## 🔍 Verifiche Utili

```bash
# Vedi cosa Git sta tracciando
git ls-files

# Vedi cosa è ignorato
git status --ignored

# Vedi dimensione repository
git count-objects -vH

# Trova file grandi
git rev-list --objects --all | \
  git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | \
  awk '/^blob/ {print substr($0,6)}' | sort --numeric-sort --key=2 | \
  tail -n 10
```

## 📚 Risorse

- [Git Documentation](https://git-scm.com/docs/gitignore)
- [GitHub .gitignore Templates](https://github.com/github/gitignore)
- [Git LFS](https://git-lfs.github.com/) - Per file grandi

---

**Ultimo aggiornamento**: 24 Ottobre 2025 - Clean Architecture Migration
